/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['ibktlbeduwmyvnvoubha.supabase.co'], // Add Supabase domain if you're loading images from there
  },
};

export default nextConfig;